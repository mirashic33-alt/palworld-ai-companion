-- Словарь регионов Palworld: код области (AreaName.Key триггера региона) -> русское имя.
-- Заполняется по мере сбора кодов из tech.log (строки "🗺 region key=...").
-- Пока пусто → событие «новая область» пишется только в tech.log (мимо модели).
-- Как коды соберутся — вписываем сюда человеческие имена, и событие пойдёт в модель.
return {
    -- ["MapArea_XXXX"] = "Ветреные холмы",
}
